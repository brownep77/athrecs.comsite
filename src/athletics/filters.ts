export * from "../lib/athrecs/filters";

/** ATHRECS is the Athletics specialist site in the SportsRecs network. */
export const SPORTS = ["Athletics"] as const;
export const DEFAULT_SPORT = "Athletics" as const;
