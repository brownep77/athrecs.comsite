// Leave time to save results before hosting termination, and never reclaim a live worker.
export const RESEARCH_TIMEOUT_MS = 540_000;
export const WORKER_MAX_DURATION_SECONDS = 600;
export const JOB_LEASE_SECONDS = 720;
