# ATHRECS authentication rollout

This branch adds a shared account chooser and configuration-driven authentication methods. The implementation enables only providers whose server-side credentials are present and keeps manual email/password hidden until verified transactional email and password recovery are configured.
